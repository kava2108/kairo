---
description: >
  プロダクトの説明から UI/UX デザインシステムを自動生成します。
  Claude ネイティブの UUPM 推論エンジンを使用し、
  スタイル・カラー・タイポグラフィ・レイアウトパターンを決定します。
  生成した design-system/MASTER.md は CEG ノードとして管理されます。
allowed-tools: Read, Write, Bash, AskUserQuestion, TodoWrite, Glob, Grep, Edit
argument-hint: "<product-description> [--stack <stack>] [--page <page-name>] [--persist] [--no-uupm] [--feature <feature>]"
---

# /kairo:design-system

## 目的

プロダクトの自然言語説明から UI/UX デザインシステムを生成し、`design-system/MASTER.md`
として永続化する。UUPM（ui-ux-pro-max-skill）の設計思想を Claude ネイティブ推論として内部化し、
業界固有のデザインパターンを選択する。kairo CEG（Conditioned Evidence Graph）の TDS フェーズノードとして統合する。

---

# context

```
PRODUCT_DESC=$ARGUMENTS  （"--" 以前のテキスト）
STACK_ARG=（--stack オプション。未指定時は --stack html-tailwind を使用）
PAGE_ARG=（--page オプション。指定時はページ別オーバーライドを生成）
FEATURE=（--feature オプション。未指定時は "main" を使用）
NO_UUPM=（--no-uupm フラグ。指定時は UUPM をスキップして手動設計モードに移行）
PERSIST=（--persist フラグ。既存 MASTER.md がある場合も上書き）

MASTER_MD=design-system/MASTER.md
COHERENCE_JSON=graph/coherence.json
```

---

# step1: コンテキスト収集

1. `.kiro/steering/*.md` を Read してプロダクトコンテキスト（技術スタック・規約）を把握する。
   ファイルが存在しない場合は、`$PRODUCT_DESC` と `$STACK_ARG` のみを使用する。
2. `$MASTER_MD` の存在を確認する。
   - **存在する場合（かつ --persist 未指定）**: 差分マージモードとして既存設計を保持し追記する。
   - **存在しない場合 / --persist 指定時**: 新規生成モード。

---

# step2: UUPM 推論（Claude ネイティブ）

> **v1.1 方針**: 外部 Python スクリプトは使用しない。
> Claude 自身が UUPM 設計思想に基づいて推論する。

`--no-uupm` フラグが指定されている場合はこのステップをスキップしてステップ3へ進む。

## step2a: 業界カテゴリ特定

`$PRODUCT_DESC` と `$STACK_ARG` を元に、以下の 10 大業界分類からカテゴリを判定する:

Tech/SaaS / Finance / Healthcare / E-commerce / Services / Creative /
Lifestyle / Education / Community / Emerging

判定結果と「ユーザーの感情的期待（信頼感・革新性・安心感 など）」を出力する。

## step2b: UUPM 推論実行

特定した業界カテゴリを踏まえ、以下の 5 次元を同時に推論して整理する:
- **Style Recommendations**: 推奨スタイル名と使用理由
- **Color Palette**: Primary / Secondary / Accent / Neutral / Semantic カラー
- **Typography**: フォントペア（見出し用 + 本文用）・フォントサイズスケール
- **Spacing**: 4px ベースグリッドに基づくスペーシングシステム
- **Component Patterns**: 業界固有の UI パターン（推薦理由付き）
- **Anti-patterns**: 避けるべき UX アンチパターン（UUPM 161ルール準拠）

---

# step3: design-system/MASTER.md 生成

`design-system/` ディレクトリを作成し、以下のフォーマットで `MASTER.md` を Write する。

```markdown
---
kairo:
  node_id: "design-system:$FEATURE"
  artifact_type: "design-system"
  phase: "TDS"
  feature: "$FEATURE"
  uupm_version: "2.5.0"
  stack: "$STACK_ARG"
  status: "approved"
  created_at: "<ISO8601 現在時刻>"
  updated_at: "<ISO8601 現在時刻>"

coherence:
  depends_on:
    - "req:$FEATURE"
  trusted_by:
    - "design:$FEATURE"
    - "imp:*"
  confidence: 1.0
---

# Design System — $FEATURE

> Generated by kairo `/kairo:design-system` using ui-ux-pro-max-skill (UUPM)
> Stack: $STACK_ARG | Feature: $FEATURE

## 1. Style Direction

<!-- UUPM 推論から抽出したスタイルの方向性 -->

## 2. Color Palette

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Primary | `--color-primary` | `#...` | CTA, Links, Focus ring |
| Secondary | `--color-secondary` | `#...` | Secondary actions |
| Accent | `--color-accent` | `#...` | Highlights, Badges |
| Neutral 100 | `--color-neutral-100` | `#...` | Background |
| Neutral 900 | `--color-neutral-900` | `#...` | Body text |
| Success | `--color-success` | `#...` | Positive states |
| Warning | `--color-warning` | `#...` | Caution states |
| Error | `--color-error` | `#...` | Error states |

## 3. Typography

| Scale | Token | Size | Weight | Line Height | Usage |
|-------|-------|------|--------|-------------|-------|
| Heading 1 | `--text-h1` | 2.5rem | 700 | 1.2 | Page titles |
| Heading 2 | `--text-h2` | 2rem | 600 | 1.3 | Section titles |
| Heading 3 | `--text-h3` | 1.5rem | 600 | 1.4 | Subsections |
| Body | `--text-body` | 1rem | 400 | 1.6 | Body text |
| Small | `--text-small` | 0.875rem | 400 | 1.5 | Captions, labels |

**Font Pair**: <!-- UUPM が推奨するフォントペアを記載 -->

## 4. Spacing System

4px ベースグリッド。`--spacing-<n>` = `n * 4px`。

| Token | Value | Usage |
|-------|-------|-------|
| `--spacing-1` | 4px | Tight spacing (icon gap) |
| `--spacing-2` | 8px | Component padding |
| `--spacing-4` | 16px | Default element spacing |
| `--spacing-6` | 24px | Section internal spacing |
| `--spacing-8` | 32px | Card padding |
| `--spacing-16` | 64px | Section spacing |

## 5. Component Patterns

<!-- UUPM 業界固有パターン -->

## 6. Layout & Breakpoints

| Breakpoint | Width | Container Max |
|-----------|-------|---------------|
| Mobile | 375px | 100% |
| Tablet | 768px | 720px |
| Desktop | 1024px | 960px |
| Wide | 1440px | 1280px |

## 7. Interaction Principles

- **Hover state**: 150–300ms transition。全インタラクティブ要素に必須。
- **Focus state**: `outline: 2px solid var(--color-primary); outline-offset: 2px;` キーボードナビ向け。
- **Cursor**: `cursor: pointer` を全クリック要素に付与。
- **Animation**: `prefers-reduced-motion` を尊重し `transition` を無効化できる設計。
- **Loading**: スケルトンUIを使用。スピナーは150ms以上の処理に限定。

## 8. Anti-patterns（避けるべきパターン）

<!-- UUPM 161ルールから抽出したアンチパターン -->

## 9. Stack-specific Implementation Notes

**Stack**: $STACK_ARG

<!-- Tailwind / CSS / スタック固有の実装ノート -->

## 10. Pre-delivery Checklist

`/kairo:implement` の UI コンポーネント完了後に自動検証するチェックリスト。

- [ ] `cursor: pointer` が全クリック要素に付与されている
- [ ] ホバーステート (150–300ms transition) が設定されている
- [ ] フォーカスステートがキーボードナビ向けに設定されている
- [ ] レスポンシブブレークポイント (375/768/1024/1440px) を満たす
- [ ] `prefers-reduced-motion` が考慮されている
- [ ] カラーコントラスト比が WCAG 2.1 AA (4.5:1) を満たす
- [ ] タイポグラフィが本ドキュメントのスケールに準拠している
- [ ] スペーシングが 4px グリッドに合っている
- [ ] コンポーネントがセクション5のパターンに準拠している
```

---

# step4: ページ別オーバーライド（--page 指定時のみ）

`$PAGE_ARG` が指定された場合:

1. `design-system/pages/$PAGE_ARG.md` を Write する。
2. フォーマット:
   ```markdown
   ---
   kairo:
     node_id: "design-system-page:$FEATURE/$PAGE_ARG"
     artifact_type: "design-system-page"
     phase: "TDS"
   coherence:
     depends_on:
       - "design-system:$FEATURE"
     confidence: 1.0
   ---
   # Page Design Override — $PAGE_ARG

   <!-- MASTER.md からのオーバーライドのみ記載。MASTER.md に準拠する部分は省略 -->
   ```

---

# step5: CEG 更新

`$COHERENCE_JSON` を Read し、以下のノードを追加または更新して Write する:

```json
{
  "node_id": "design-system:$FEATURE",
  "artifact_type": "design-system",
  "phase": "TDS",
  "file": "design-system/MASTER.md",
  "depends_on": ["req:$FEATURE"],
  "trusted_by": ["design:$FEATURE"],
  "confidence": 1.0,
  "status": "APPROVED"
}
```

`$MASTER_MD` のファイルパスを `graph/coherence.json` に登録する。

---

# step6: サマリー出力

完了後、以下のサマリーを出力する:

```
✅ design-system/MASTER.md を生成しました

📦 スタック    : $STACK_ARG
🎨 スタイル    : <推奨スタイル名>
🎯 カラーパレ  : Primary=<hex>, Secondary=<hex>
🔤 フォントペア: <見出しフォント> / <本文フォント>
🔗 CEG ノード  : design-system:$FEATURE

次のステップ:
  /kairo:spec-design $FEATURE   ← design.md に Design System セクションを追加
  /kairo:implement <issue-id>   ← UI 実装時に MASTER.md を自動参照
```
